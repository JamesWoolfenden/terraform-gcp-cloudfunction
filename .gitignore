# Compiled files
*.tfstate
*.tfstate.backup
*.zip
.DS_Store
# Module root lock file — this repo is a reusable module, not a root config,
# so provider pinning belongs to callers/examples, not here. Example/template
# lock files (e.g. example/*/.terraform.lock.hcl) are intentionally kept.
/.terraform.lock.hcl
# Module directory
.terraform/
.terraform
.plugins
__pycache__/
.vscode/
.idea
*.iml
*.orig
*.ini
*~HEAD
*backup
tmp
lambda
*~HEAD
*backup
*.bak
.terraform.tfstate.lock.info
tf.json
tf.plan
*.log
output/*.json
secrets.auto.tfvars
checksums.txt
*.tar.gz
original/
.external_modules
